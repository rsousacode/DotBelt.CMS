{
  "name": "boilerplatessr",
  "version": "1.0.0",
  "scripts": {
    "dev": "npm run docker-up-detached; npm run frontend;",
    "docker-up": "docker compose up --build",
    "docker-up-detached": "docker compose up --build -d",
    "frontend": "cd BoilerPlate.Frontend && npm run dev",
    "docker-preview": "docker compose -f docker-compose.all.yml up  --build --remove-orphans"
  },
  "author": "",
  "license": "ISC",
  "description": ""
}
